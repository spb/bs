ifndef BS__INIT_MK
BS__INIT_MK=1

BUILDDIR ?= build
TMPROOT ?= intermediate

TARGET_VARIABLES = CXXFLAGS LDFLAGS SOURCES LIBRARIES SRCDIRS
SUBDIR_VARIABLES = CXXFLAGS LDFLAGS DSOS EXECUTABLES SUBDIRS SRCDIRS

DIRS += $(BUILDDIR) $(TMPROOT) $(BUILDDIR)/lib $(BUILDDIR)/bin

# Default values for variables that can later be added to
CPPFLAGS = -I.

endif
