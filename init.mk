ifndef BS__INIT_MK
BS__INIT_MK=1

BUILDDIR ?= build
TMPDIR ?= intermediate

PER_TARGET_VARIABLES = CXXFLAGS LDFLAGS SOURCES
SUBDIR_VARIABLES = CXXFLAGS LDFLAGS DSOS EXECUTABLES

DIRS += $(BUILDDIR) $(TMPDIR) $(BUILDDIR)/lib $(BUILDDIR)/bin


endif
